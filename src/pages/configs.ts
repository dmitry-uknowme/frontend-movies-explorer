export const xlScreenParams = {
  initialCount: 16,
  incrementByCount: 4,
};

export const lgScreenParams = {
  initialCount: 12,
  incrementByCount: 3,
};

export const mdScreenParams = {
  initialCount: 8,
  incrementByCount: 2,
};

export const smScreenParams = {
  initialCount: 5,
  incrementByCount: 2,
};

export const SHORT_MOVIE_LEN = 40;
